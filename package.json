{
  "scripts": {
    "test": "web-ext lint",
    "posttest": "eslint src/",
    "start": "web-ext run",
    "build": "web-ext build"
  },
  "webExt": {
    "sourceDir": "src/",
    "build": {
      "overwriteDest": true
    }
  }
}
